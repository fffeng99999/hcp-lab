#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export NODES_LIST="${NODES_LIST:-16,32,64,128,256}"
export TX_COUNT="${TX_COUNT:-100,1000,10000}"
export REPEAT="${REPEAT:-5}"
export DIFFICULTY="${DIFFICULTY:-8}"
export TARGET_BLOCK_MS="${TARGET_BLOCK_MS:-2000}"
export TX_PER_BLOCK="${TX_PER_BLOCK:-100}"
export TARGET_TPS="${TARGET_TPS:-10}"
export BATCH_SIZE="${BATCH_SIZE:-100}"
export ORPHAN_BASE_RATE="${ORPHAN_BASE_RATE:-0.01}"
export PORT_OFFSET="${PORT_OFFSET:-7000}"
export CHAIN_ID="${CHAIN_ID:-hcp-exp7}"
export LOADGEN_DB_ISOLATION="${LOADGEN_DB_ISOLATION:-true}"
export LOADGEN_DB_RESET="${LOADGEN_DB_RESET:-true}"
export LOADGEN_DB_SCHEMA_PREFIX="${LOADGEN_DB_SCHEMA_PREFIX:-exp7}"

bash "$SCRIPT_DIR/test_exp7_pow.sh"
