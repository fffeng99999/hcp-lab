#!/bin/bash
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export COMETBFT_ORIGINAL_NODES="${COMETBFT_ORIGINAL_NODES:-8,16,32}"
export COMETBFT_ORIGINAL_TXS="${COMETBFT_ORIGINAL_TXS:-1000}"
export COMETBFT_ORIGINAL_REPEAT="${COMETBFT_ORIGINAL_REPEAT:-5}"
export COMET_SIGNED_TXS="${COMET_SIGNED_TXS:-true}"

python3 "$SCRIPT_DIR/run_cometbft_compare.py" "$@"
